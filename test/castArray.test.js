import castArray from "../src/castArray";
//const castArray = require('../src/castArray');

describe("Calculator tests", () => {
    test('adding 1 + 2 should return 3', () => {
      // arrange and act
      var result = castArray(true);
    
      // assert
      expect(result).toEqual([true]);
    });
});